import "../global.dart";

class SettingingsPageContainer extends StatelessWidget {
  const SettingingsPageContainer({Key? key}) : super(key: key);
   
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 500,
      height: 90,
      color: Colors.white,
    );
  }
}